name: 'Run action'

on:
  push:
    branches:
      - '**'
    paths:
      - '.github/workflows/**'
      - 'src/**'
      - 'dist/**'
      - 'package*.json'
      - 'action.json'

jobs:
  run-action:
    name: 'Run action'

    strategy:
      matrix:
        system:
          - windows-latest
          - ubuntu-latest
          - macos-latest

    runs-on: ${{ matrix.system }}

    steps:
      - name: Setup Ninja
        id: setup-ninja
        uses: imesense/gha-setup-ninja@1fd7d9872c94b7dde9afdca4a796df8c68806ac3
        with:
          version: '1.12.1'

      - name: Print version
        run: |
          ninja --version
